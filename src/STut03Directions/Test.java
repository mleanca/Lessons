package STut03Directions;
public class Test {
    public static void main(String[] args) {

        /*
        Directions[] directions = Directions.values();
        //ClassEnum[] object = method;
        for (int i = 0; i < directions.length; i++) {
        //FOR LOOP statement
            // statement 1: int i = 0 sets a variable before the loop starts
            // statement 2: defines the condition for the loop to run (i must be less than the number of variables in an enumeration or array). If the condition is true, the loop will start over again, if it is false, the loop will end.
            // statement 3: increases a value (i++) each time the code block in the loop has been executed. i++ - post increment to use current value in statement and increment value by 1. ex. 0 1 2 3

            System.out.println(directions[i] + " ");
        }
         //outputs UP, RIGHT, DOWN, LEFT
         //outputs only RIGHT, DOWN, LEFT if i = 1, i < directions.length, i++
         //outputs only DOWN, LEFT if = 2,
         //nothing is i > directions.length
        */

        /*How to access RIGHT from enumeration
        Directions directions = Directions.RIGHT;
        System.out.println(Directions.RIGHT); //RIGHT
        System.out.println(directions == Directions.RIGHT); //true
         */

    }
}
